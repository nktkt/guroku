# v1.2 release checklist

## Implementation
- [x] Add `pubgrub = "0.2"` to `Cargo.toml`.
- [x] Bump crate version to `1.2.0`.
- [x] New module `src/pubgrub_resolver.rs` with `NpmVersion` newtype and `resolve_with_pubgrub` entry point.
- [x] Two-phase async-prefetch + sync-solve design documented in module-level comments.
- [x] Wire `commands::install::run` to call `pubgrub_resolver::resolve_with_pubgrub` by default.
- [x] `GUROKU_RESOLVER=bfs` env var to force the v1.1 BFS path.
- [x] file:/git: roots fall back to the v1.1 BFS resolver.

## Tests
- [x] `tests/pubgrub_npm_version.rs` — Version trait impl semantics.
- [x] `tests/pubgrub_resolver_simple.rs` — public-surface compile checks.
- [ ] `tests/pubgrub_diamond_conflict.rs` — diamond conflict where v1.1 single-step backtrack would fail.
- [ ] `tests/pubgrub_cascade_backtrack.rs` — cascading backtrack (one substitution forces another).
- [ ] `tests/pubgrub_conflict_report_format.rs` — `ResolutionConflict.requested_by` carries the pubgrub report.
- [ ] `tests/pubgrub_alias_root.rs` — pubgrub honours `npm:<real>@<spec>` at the root.
- [ ] `tests/pubgrub_overrides_applied.rs` — root-level overrides land in the prefetched plan.
- [ ] `tests/pubgrub_file_root_falls_back.rs` — file: root drops back to BFS.
- [ ] `tests/pubgrub_git_root_falls_back.rs` — git: root drops back to BFS.
- [ ] `tests/pubgrub_resolver_smoke_lib.rs` — library-side smoke + import test.
- [x] Existing v1.1 tests (`tests/resolver_no_backtrack.rs`, `tests/overrides_*`, `tests/specs_*`) still pass against the v1.1 path.

## Stability
- [x] `guroku::prelude` items unchanged.
- [x] CLI surface unchanged (no new subcommands; no new flags).
- [x] Lockfile schema unchanged. v1.0/v1.1 lockfiles read by v1.2 unchanged.
- [x] `Resolved` struct unchanged from v1.1 (`aliased_from` field already present).

## Docs
- [ ] `docs/v1.2-release-notes.md`.
- [ ] `docs/migration/v1.1-to-v1.2.md`.
- [ ] `docs/internals/pubgrub-integration.md`.
- [ ] `docs/internals/range-conversion.md`.
- [ ] `docs/contributing/v1.2-features-overview.md`.

## Repo hygiene
- [ ] `CHANGELOG.md` `[1.2.0]` section.
- [ ] `README.md` status line bumped to v1.2.
- [ ] `ROADMAP.md` v1.2 marked Shipped.
- [ ] ASCII assets: `assets/v1.2-banner.txt`, `assets/pubgrub-flow.txt`.
- [ ] PR template variant `.github/PULL_REQUEST_TEMPLATE/pubgrub_change.md`.
- [ ] Issue template `.github/ISSUE_TEMPLATE/pubgrub_resolution_failure.yml`.
- [ ] CI workflow `.github/workflows/pubgrub-fuzz.yml` runs the new resolver tests.

## Cut + ship
- [ ] `cargo build`/`clippy --all-targets -D warnings`/`fmt --check` clean.
- [ ] `cargo test --tests` passes.
- [ ] Commit on `main`.
- [ ] Tag `v1.2.0` and push (release.yml builds binaries).

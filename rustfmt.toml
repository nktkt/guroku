# rustfmt is the ground truth for code style in this project.
# Only stable rustfmt options are used here; no nightly-only settings.

edition = "2021"
max_width = 100
newline_style = "Unix"
hard_tabs = false
tab_spaces = 4
use_field_init_shorthand = true
use_try_shorthand = true

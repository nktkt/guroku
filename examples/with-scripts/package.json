{
  "name": "guroku-example-with-scripts",
  "version": "0.1.0",
  "description": "Example: package.json#scripts and lifecycle hooks under guroku v0.4.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "preinstall": "echo '[preinstall] starting'",
    "postinstall": "echo '[postinstall] complete'",
    "build": "echo 'pretend tsc invocation'",
    "test": "echo 'pretend test runner'",
    "say-hi": "printf 'hello from %s\\n' \"$1\"",
    "list-args": "printf '%s\\n' \"$@\""
  },
  "dependencies": {
    "ms": "^2.1.0"
  }
}
